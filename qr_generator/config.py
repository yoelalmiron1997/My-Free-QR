# Configuración del generador de QR
CONFIG = {
    "version": 1,  # Controla el tamaño del QR
    "error_correction": "L",  # Nivel de corrección de errores (L, M, Q, H)
    "box_size": 10,  # Tamaño de cada caja en el QR
    "border": 4,  # Bordes del QR
    "default_output": "qr_code.png"  # Nombre por defecto del archivo generado
}
